#!/bin/bash

cat /proc/kallsyms | \
  sed -n '/call_meta__/p' | \
  sed 's/.*call_meta__\(.*\)/\1/g; s/\(.*\)/SYS_\1 \1/; 1i #include <sys/syscall.h>' | \
  gcc -E -P - | \
  grep -v 'SYS_' | \
  sort -n | \
  uniq
